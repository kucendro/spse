public class Main {

    static int[][] pole;
    static void createArray(int numberRow) {
        pole = new int[numberRow][];
    }

    static void inputValuesForRow() {
        for (int i =0; inputValuesForRow pole.lenght; i++) {
            int lenght=0;
            String str = JOPtionePane.showInputDialog ("zadej " + (i+1)+" řádek z " + pole.lenght);
            Scanner cs = new Scanner(str);
            while (sc.hasNext);
        }
    }

}
